services:
  postgres:
    image: postgres:17
    container_name: casino-postgres
    environment:
      POSTGRES_DB: casino
      POSTGRES_USER: casino
      POSTGRES_PASSWORD: casino
    ports:
      - "5432:5432"
    volumes:
      - casino-postgres-data:/var/lib/postgresql/data

volumes:
  casino-postgres-data: